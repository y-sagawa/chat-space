.side
  .side-header
    .side__user
    = current_user.name
    .icon-new-groups
      = link_to new_group_path do
        %i.fas.fa-edit
    .icon-edit
      = link_to edit_user_path(current_user) do
        %i.fas.fa-cog
  .side-content
    - current_user.groups.each do |group|
      = link_to group_messages_path(group) do
        .side-content__groupname
          = group.name
        .side-content__groupmessages ああああああああああああああああああああああああああああ
